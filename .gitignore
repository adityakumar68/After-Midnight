# Per global rule: never commit superpowers spec files
docs/superpowers/

# Next.js
.next/
out/
build/
dist/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Misc
.DS_Store
*.log
.vercel
.turbo
.cache

# Editor
.vscode/
.idea/
*.swp

# Audio assets are committed so Vercel deploys them.
# Re-bake with `npm run bake:songs` and `npm run bake:sfx` to regenerate.

# HyperFrames demo-video work — keep the FINAL render at repo root as demo.mp4
# instead. The composition + intermediate audio/clips/scripts stay local.
media/video/
recordings/
scripts/bake-demo-assets.ts
scripts/bake-vo-chunks.ts

# Raw cover-image variants (Gemini export + intermediate dewatermark files).
# The final compressed cover lives at public/cover.jpg.
cover-image*.png
cover-image-clean*

# Raw Gemini-generated images (renamed copies live in media/video/assets/)
images-midnight/

# User's local content drop folder (sources for AI-generated assets)
midnight-content/

# Raw nano-banana logo source dir (cleaned versions live in public/logo/)
logo-content-nano-banana/
